#include <bits/stdc++.h>
using namespace std;
void solve(string str,int index,string result,unordered_set<int>&seti){
    if(index>=str.size()){
    }
    seti.insert(result);
}

int main() {
    string s;
    cout<<"Enter the string"<<endl;
    cin>>s;
    int index=0;
    string result="";
    solve(s,index,result,seti);

    return 0;
}